export default class Task {
    constructor(name, description, dueDate, priority, complete) {
        this.name = name;
        this.description = description;
        this.dueDate = dueDate;
        this.priority = priority;
        this.complete = complete;
    }   
}